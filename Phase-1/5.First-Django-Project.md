# 🎥 Django Project Setup: Create Project & Run Server

Here’s a step-by-step tutorial to create a Django project and run the development server.

---

## ✅ **Prerequisites**
- Python installed (3.8 or later)
- `pip` available
- Terminal/Command Prompt ready

---

## 📦 **Step 1: Install Django**
```bash
pip install django
```
*Verify installation:*
```bash
django-admin --version
```

---

## 🏗 **Step 2: Create a Django Project**
```bash
django-admin startproject myproject
```
Replace `myproject` with your desired project name (e.g., `blog`, `portfolio`).

✅ This creates a folder with your project name containing Django's default structure.

---

## 📁 **Step 3: Understand the Project Structure**
Navigate into your project:
```bash
cd myproject
```

The structure looks like:
```
myproject/
│
├── manage.py           # Command-line utility
└── myproject/          # Project configuration folder
    ├── __init__.py
    ├── settings.py     # Project settings
    ├── urls.py         # URL declarations
    ├── asgi.py         # ASGI config
    └── wsgi.py         # WSGI config
```

### 📝 Key Files Explained:
- **manage.py**: Used to run commands (start server, create apps, run tests).
- **settings.py**: Database, apps, middleware, templates configuration.
- **urls.py**: URL routing for the project.

---

## 🚀 **Step 4: Run the Development Server**
```bash
python manage.py runserver
```

You'll see output like:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

---

## 🌐 **Step 5: View Your Site**
1. Open your browser
2. Go to: **http://127.0.0.1:8000**
3. You should see the Django welcome page:  
   ![Django Welcome](https://www.djangoproject.com/s/img/logos/django-logo-positive.png)

---

## ⚙️ **Step 6: (Optional) Change Port or IP**
By default, server runs on `127.0.0.1:8000`.

**To change port:**
```bash
python manage.py runserver 8080
```

**To make it accessible on your network:**
```bash
python manage.py runserver 0.0.0.0:8000
```
Then access via your local IP from other devices.

---

## 📌 **Step 7: Stop the Server**
Press **CTRL + C** in the terminal where the server is running.

---

## 🛠 **Step 8: Apply Migrations (Initial Setup)**
*Before creating your own models, apply default migrations:*
```bash
python manage.py migrate
```
This sets up the default database (SQLite).

---

## 🧪 **Step 9: Create a Superuser (Optional but Recommended)**
```bash
python manage.py createsuperuser
```
Follow prompts to create an admin account, then visit:  
**http://127.0.0.1:8000/admin**

---

## 📋 **Quick Recap Checklist**
1. ✅ `pip install django`
2. ✅ `django-admin startproject projectname`
3. ✅ `cd projectname`
4. ✅ `python manage.py migrate`
5. ✅ `python manage.py runserver`
6. ✅ Visit `http://127.0.0.1:8000`
7. ✅ (Optional) Create superuser for `/admin`

---

## ⚠️ **Common Issues & Solutions**
| Problem | Solution |
|---------|----------|
| **Port already in use** | `runserver 8080` or kill process using port 8000 |
| **ModuleNotFoundError** | Ensure you're in the correct directory (where `manage.py` exists) |
| **Django not installed** | Check with `pip list \| grep Django` |
| **Windows `python` command not found** | Try `py manage.py runserver` or use `python3` |

---

## 🎯 **Next Steps After Project Creation**
1. **Create an app**: `python manage.py startapp myapp`
2. **Add app to `INSTALLED_APPS` in `settings.py`**
3. **Define models in `myapp/models.py`**
4. **Create views and URLs**
5. **Run `makemigrations` and `migrate` for custom models**

---

**Your Django project is now running!** 🎉 
The development server automatically reloads when you modify code, making it perfect for development.

